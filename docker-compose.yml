version: '3.3'

services:
  postgres:
    image: postgres
    environment:
      - POSTGRES_DB=my_store
      - POSTGRES_USER=jess
      - POSTGRES_PASSWORD=admin
    ports:
      - 5432:5432
    volumes:
      - /postgres-data:/var/lib/postgresql/data
